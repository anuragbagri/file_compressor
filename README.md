# Simple File Compressor (Rust)

A simple file compressor built using the [`flate2`](https://crates.io/crates/flate2) crate in Rust.

## 🚀 How to Run

1. Clone this repository:
2. move the file to be compressed inside of cloned repo folder.
3. run
   ```bash
   cargo run file_name.txt compressed_file.txt
   ``` 
